import React from "react";

import ErrorBtnCSS from "./ErrorButton.module.css";

const ErrorButton = () => {
  return <button className={ErrorBtnCSS.btn}>Back to Home</button>;
};

export default ErrorButton;
